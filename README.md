
  # Design Splash Screen Variants

  This is a code bundle for Design Splash Screen Variants. The original project is available at https://www.figma.com/design/wnuqllSO7hwhl97NRLJbTc/Design-Splash-Screen-Variants.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  